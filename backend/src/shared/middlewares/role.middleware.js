/**
 * Re-exportación del middleware de autorización por roles
 * Este archivo existe para mantener compatibilidad con imports que buscan role.middleware.js
 */
export { authorize } from './auth.middleware.js';
